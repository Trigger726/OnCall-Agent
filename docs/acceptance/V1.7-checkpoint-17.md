# V1.7 checkpoint-17：Redis Stream 重建后的通知游标恢复

日期：2026-09-12。状态：修复前后对照、真实 Redis 恢复、双 JVM 回归及六项 CI 门禁通过。

本地 `./mvnw.cmd -B package` 已通过：75 项发现、66 项执行通过、9 项 Docker 条件测试跳过，JAR 打包成功，耗时 48.269 秒。不能将跳过的 Redis 测试计作真实恢复已通过。

## 缺陷与设计依据

基线 `1157e4f` 的 AgentEventReceiver 只按内存游标 XREAD。根据 [Redis XREAD](https://redis.io/docs/latest/commands/xread/) 的严格大于游标语义，若 Stream 被重建且 ID 小于或等于旧游标，保留原接收器的情况下可能一直空读。业务 SSE 仍有数据库兜底，但不能把兜底可用等同于通知通道已恢复。

先新增 8 项单元场景，旧生产代码失败 6 项，均缺少第二条通知；[修复前失败摘要](assets/V1.7-checkpoint-17/receiver-before-fix.txt) 已保留。修复后相同 8 项通过，覆盖毫秒/序号回退、超过有符号 long 上限的合法 Redis ID、同 ID 不同字段、观察到空 Stream、尾记录未变及检查异常。

## 实现与边界

- 仅在 XREAD 空读且当前游标非 `0-0` 时执行 [XREVRANGE](https://redis.io/docs/latest/commands/xrevrange/) COUNT 1，最多检查一条尾通知；正常有数据读取不增加尾检查。
- 尾部为空、尾 ID 数值小于游标，或尾 ID 相同但字段被替换时，将**传输游标**重置为 `0-0`，下一次调度开始重读。ID 按两段无符号 64 位数比较，不按字符串或有符号 long 排序。
- 保留每轮 COUNT 100。不在检测轮循环全量回放，不修改数据库事件、outbox 或浏览器游标。重复通知依然由已有数据库有序补读消化。
- 查询异常或无可用响应不触发重置。普通尾记录未变时不重复回放。删除尾记录也可能引起保守回读，属于允许的重复提示。
- 这是可观测尾部变化的恢复，不是 Stream 世代协议：重建后尾部已超过旧游标、或相同 ID/字段碰巧复用时，不能据此识别所有历史缺口。已丢失/裁剪的提示仍靠数据库补读，不能关闭兜底，也不能声称 Redis 数据恢复或 exactly-once。

## 真实 Redis 验证设计

RedisRelayIntegrationTest 新增两个使用独立随机测试 key 的场景，沿用真实 Redis 7.4 Testcontainers。测试只删除自己创建的 key，不操作用户 Redis。

1. 同一个 receiver 对象先接收 `100-10`，删除/重建为 `100-9`，再删除/复用 `100-9` 携带不同 eventId，最后观察 key 消失并重建为 `1-0`。必须依次收到 eventId 1、2、3、4，无重启、无数据库兜底。
2. 旧游标 `1000-0` 后重建 205 条较低 ID，检测轮不发布，后续三轮分别发布 100、100、5 条。裁剪到仅保留尾记录后空读两轮不能重放；新追加第 206 条仍可收到。

CI Redis 门禁新增 `redis-agent-evidence` artifact，保存两个场景的实际事件列表及 Surefire 报告。手动调用 poll 的接收器组件测试不等同于 JVM 重启或浏览器故障演练；checkpoint-16 双 JVM 调度与暂停恢复门禁继续回归。

## 真实运行证据

修复提交 `ad8bb6d` 的 [CI 34665811513](https://github.com/Trigger726/OnCall-Agent/actions/runs/34665811513) 已取得以下产物：

六项门禁全部成功：前端 17 秒、H2/JAR 53 秒、MySQL 51 秒、Redis 37 秒、双 JVM 1 分 33 秒、容器构建/启动 1 分 5 秒。远端默认后端同样发现 75 项、66 项通过、9 项跳过。artifact v4 的 Node 20 弃用提示仍存在，但本次上传成功；依赖维护单独跟进。

- Redis 门禁执行 4 项、0 失败/错误/跳过，测试耗时 13.50 秒。[重建结果](assets/V1.7-checkpoint-17/ci-34665811513-recreation.json) 确认同一 receiver 收到 1、2、3、4；[有界恢复结果](assets/V1.7-checkpoint-17/ci-34665811513-bounded-recovery.json) 确认 100/100/5 三轮，裁剪后计数保持 206，追加后计数变为 207。
- 双 JVM 回归执行 2 项、0 失败/错误/跳过，耗时 67.62 秒。[正常结果](assets/V1.7-checkpoint-17/ci-34665811513-distributed-normal.json) 在关闭定期补读时 A/B/数据库各 20 条事件一致；[暂停结果](assets/V1.7-checkpoint-17/ci-34665811513-distributed-outage.json) 同样三份列表一致，恢复后待投递 outbox 从 15 条自动归零。与 checkpoint-16 的 18 条不同是故障注入时投递进度不同，两次均满足暂停期间存在积压、恢复后自动清空的断言。
- 新增 JSON 从 CI artifact 核对归档，不覆盖历史结果。它们分别证明组件重建恢复和原有两实例暂停恢复，不能合并宣称“两实例 Redis 重启端到端已验收”。

## Demo 对照与遗留项

保留 checkpoint-14 旧/新桌面和移动端截图、checkpoint-15 双 JVM 正常结果、checkpoint-16 Redis 暂停结果。本轮是后台可靠性修复，新增测试证据对照，不伪造页面差异。

仍待覆盖：真实 Redis 进程重启/持久化丢失的端到端矩阵、真实慢 socket、浏览器刷新后自动挂接、OnCall 独立实页恢复、执行 JVM 崩溃后任务结算，以及 checkpoint-16 已发现的客户端断连日志分级。项目整体目标保持进行中。
