# V1.7 checkpoint-25：跨 JVM CLIENT/SERVER Trace 拼接

日期：2026-09-19。状态：通过。独立 fixture JAR/协议、本机静态检查、真实 Docker/Tempo 跨服务父子关系与 Run 58 七项 CI 全部通过。V1.7 整体仍在持续迭代。

## 前一检查点留下的问题

checkpoint-24 已用真实 TCP 端点证明 OpsPilot 发出的 `traceparent` 与 HTTP CLIENT span 一一对应，但端点本身没有 tracing SDK，也没有把 SERVER span 导入 Tempo。它只证明发出端正确，不能证明分布式 Trace 在服务边界另一侧接续。

## 实现与范围

- 新增独立 Maven/Spring Boot 应用 `integration/trace-provider-fixture`。两个只读 HTTP 端点分别返回合法 Prometheus vector 与 Loki streams 响应；Spring Boot/Micrometer/OpenTelemetry 对请求自动产生 SERVER span，并经同一 Collector 导出。fixture 只在 Compose `tracing-test` profile 启动，不进入 OpsPilot 生产 JAR，也不在普通 `tracing` 演示中启动。
- CI Trace 脚本在隔离的 Compose project 中开启 OpsPilot Prometheus/Loki Provider，令其真实访问 fixture；默认 `docker compose up` 的 Provider 开关仍关闭。
- 从 Tempo 原始 `batches[].resource.attributes` 提取两个服务的 `service.name`，检查恰有 2 个 `opspilot.provider.query`、2 个 OpsPilot `SPAN_KIND_CLIENT` 和 2 个 fixture `SPAN_KIND_SERVER`；两个 CLIENT 必须分别隶属两个 Provider，SERVER 的两个 parentSpanId 必须与 CLIENT 的两个 spanId 一一相等，SERVER 路径必须分别为指标和日志端点，所有相关 span 必须共享唯一 traceId。
- 同一断言分别用于正常调查与 Tempo 短暂停机后恢复的调查；原有 `1 run / 6 tool / 2 provider`、Grafana 读回、业务 `COMPLETED`/健康 `UP`、Collector 真实失败日志、敏感哨兵排除均保留。

## 当前直接证据

- `.\mvnw.cmd -f integration/trace-provider-fixture/pom.xml -DskipTests package`：`BUILD SUCCESS`，生成独立可执行 JAR。
- 本机独立 JVM 启动后，`/actuator/health` 为 `UP`；两个 Provider 端点分别返回 `success/vector/1` 与 `success/streams/1`。
- `docker compose --profile tracing --profile tracing-test config --quiet` 与 `bash -n scripts/verify-tracing-pipeline.sh`：静态检查通过。
- 本机 Docker Desktop daemon 不可用，故本地静态/JAR 结果没有被当作容器结论；远端 GitHub Actions 是真实容器证据。
- 首轮真实容器 [Run 56](https://github.com/Trigger726/OnCall-Agent/actions/runs/35417625738) 七项作业全绿；下载 tracing artifact 后复核正常与 Tempo 故障恢复均为 `1/6/2/2/2`，两个独立服务的 parent ID 一一匹配。复核后又补充“两个 CLIENT 必须分别属于两个 Provider、两个 SERVER 必须分别覆盖指标/日志路径”的反例约束。
- 加严后的 [Run 57](https://github.com/Trigger726/OnCall-Agent/actions/runs/35417872893) Tracing job `105829894117` 通过，正常与故障恢复 artifact 仍均为 `1/6/2/2/2`。同次运行的双 JVM SSE job `105829894068` 在“恢复终态事件到 Redis”断言失败，容器镜像因此跳过。失败日志表明恢复协调器已结算 run，但测试只等 20 秒 outbox 排空；生产 `AgentOutboxRelay` 使用 30 秒 claim lease，执行节点在领取后崩溃时，幸存节点无法在 20 秒内合法重领。测试等待改为有界 50 秒以覆盖租约到期与调度，不改变业务租约或降低最终断言；Run 58 已复验通过。

## 最终远端验收

[Run 58](https://github.com/Trigger726/OnCall-Agent/actions/runs/35418126716) 对修复提交 `395e2a9` 七项作业全绿：

| 作业 | Job ID | 结果 |
| --- | --- | --- |
| Frontend build | [105830604034](https://github.com/Trigger726/OnCall-Agent/actions/runs/35418126716/job/105830604034) | 通过 |
| Backend tests and JAR (H2) | [105830604118](https://github.com/Trigger726/OnCall-Agent/actions/runs/35418126716/job/105830604118) | 通过 |
| MySQL 8.4 integration | [105830604047](https://github.com/Trigger726/OnCall-Agent/actions/runs/35418126716/job/105830604047) | 通过 |
| Redis Streams relay integration | [105830604033](https://github.com/Trigger726/OnCall-Agent/actions/runs/35418126716/job/105830604033) | 通过 |
| Two JVM Agent SSE integration | [105830603938](https://github.com/Trigger726/OnCall-Agent/actions/runs/35418126716/job/105830603938) | 通过 |
| OpenTelemetry Collector and Tempo integration | [105830604068](https://github.com/Trigger726/OnCall-Agent/actions/runs/35418126716/job/105830604068) | 通过 |
| Container image build and smoke test | [105830992379](https://github.com/Trigger726/OnCall-Agent/actions/runs/35418126716/job/105830992379) | 通过 |

原始 `tracing-pipeline-evidence` artifact 包含两次完整 Tempo trace、`cross-service-spans.json`、Collector 失败日志、Grafana 读回和 Compose 日志。正常与 Tempo 停机恢复分别得到不同 traceId，均为 `1 run / 6 tool / 2 provider / 2 client / 2 server`；两条 SERVER 路径分别是 `/api/v1/query` 与 `/loki/api/v1/query_range`，各自 parentSpanId 与对应 CLIENT spanId 精确一致，CLIENT parentSpanId 又与不同 Provider spanId 一致。精简结果和 Base64 ID 配对归档于 [`assets/V1.7-checkpoint-25/normal-result.json`](assets/V1.7-checkpoint-25/normal-result.json)、[`outage-result.json`](assets/V1.7-checkpoint-25/outage-result.json) 与 [`span-pairs.json`](assets/V1.7-checkpoint-25/span-pairs.json)。

Run 58 的 `distributed-agent-evidence` artifact 还证明崩溃恢复最终为 `TIMED_OUT`、终态事件恰一条、B 端 SSE ID 与数据库 ID 全量一致、`pendingOutboxAfterRecovery=0`；Run 57 的失败日志保留，不把复验通过抹成“从未失败”。

## 可证伪验收

1. Compose 启动 OpsPilot、独立 fixture、Collector、Tempo、Grafana；fixture 和 OpsPilot 健康均为 `UP`。
2. 一次真实告警的六工具调查返回 `COMPLETED`，TraceQL 按 run ID 找到 trace，Grafana 读回同一 trace。
3. Tempo 内同时存在两个不同 `service.name`；两个 HTTP CLIENT span 必须分别由两个 `opspilot.provider.query` 直接派生，两个 fixture SERVER span 必须分别覆盖指标/日志路径并以这两个 CLIENT spanId 为 parent，traceId 只能有一个。
4. 停止 Tempo 后再次完成调查，OpsPilot 仍健康，Collector 确实记录导出失败；Tempo 恢复后必须重新满足上述跨服务父子断言。
5. 两次 trace 都不得包含动态敏感哨兵。任一条不满足则本检查点不通过，不用“都能查到 span”替代严格父子关系。

## 新旧 Demo 对比

| 能力 | checkpoint-24 | checkpoint-25 |
| --- | --- | --- |
| 接收端 | 单进程测试中的 HTTP server | 独立 JVM 和 Docker 容器 |
| Trace 证据 | header 与内存 exporter 一一对应 | 两个服务的 CLIENT/SERVER span 写入同一 Tempo trace |
| 故障恢复 | OpsPilot 自身 span 恢复 | 两服务 span 在 Tempo 恢复后仍能按 parent ID 拼接 |

历史 checkpoint-21～24 的 SDK、协议、Tempo 正常与短暂停机演示继续保留。

## 边界

- fixture 返回兼容协议，但不是生产 Prometheus/Loki 集群，不宣称验证其真实 tracing instrumentation、TLS、网关或多租户配置。
- Collector 当前仅内存发送队列；本轮只覆盖其进程存活时的 Tempo 短暂停机，不保证 Collector 重启后零丢失。
- 生产采样率、跨信任边界 context 策略、对象存储与高并发成本仍待验证。

## 依据

- [OpenTelemetry Context propagation](https://opentelemetry.io/docs/concepts/context-propagation/)：通过 W3C `traceparent` 跨进程传输 traceId 和父 spanId。
- [OpenTelemetry Trace API](https://opentelemetry.io/docs/specs/otel/trace/api/)：传递 CLIENT span context 后，远端 SERVER span 通常是其子节点。
- [Spring Boot 3.2 tracing](https://docs.spring.io/spring-boot/docs/3.2.x/reference/html/actuator.html#actuator.micrometer-tracing)：自动 HTTP 观测和 OTLP 配置依据。
