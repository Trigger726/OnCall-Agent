# OpsPilot V1.7 验收报告：检查点 05

## 1. 基本信息

- 验收日期：2026-09-05
- 验收主题：MySQL REPEATABLE READ 快照可见性与 Problem 并发提升
- 当前状态：本地 H2 全量回归和 JAR 通过；首次真实 MySQL 门禁失败已定位并完成第二版修复，等待第二轮远端确认
- 改动范围：唯一键冲突完整恢复事务、MySQL 双事务测试、架构/面试/演示文档

## 2. 风险与设计

checkpoint 04 已确认 H2 双线程只创建一个 Problem，但 MySQL 用例当时只顺序执行。`ProblemService.create` 的旧路径先普通查询、再插入；唯一键冲突后仍用普通查询回读。依据 [MySQL 8.4 一致性非锁定读](https://dev.mysql.com/doc/refman/8.4/en/innodb-consistent-read.html)，REPEATABLE READ 的普通读取使用事务内既有快照，存在继续看不到另一事务刚提交行的风险。

第一版将冲突后的主键回收改为 `SELECT ... FOR UPDATE` 当前读，但真实 MySQL 证明这只能看见赢家主键：随后关联同步里的普通查询仍使用失败事务先前建立的旧快照。最终方案是在捕获唯一键冲突后暂停原事务，用 `REQUIRES_NEW` 新事务整体执行赢家回收、缺失关联同步和完整视图读取。无竞争主路径保持不变；数据库唯一约束继续承担最终一致性，代码不引入 JVM 本地锁，因此多实例部署语义不变。

## 3. 确定性测试

真实 MySQL 条件测试新增第二个独立用例：

1. 在已提交数据中准备同服务、同指纹、跨两个 Incident 的候选。
2. 启动两个显式 REPEATABLE READ 事务，各自先查询并断言 Problem 不存在，从而确定性建立旧快照。
3. 两个事务到达闸门后同时调用真实 `ProblemService.create`，所有屏障和 Future 均设置等待上限。
4. 要求结果恰好一次创建、一次复用，两个响应返回同一 Problem；新关联数为 2 和 0。
5. 提交后按复发键、Problem 主键和时间线证据引用查询，要求一条 Problem、两条关联、两条 `PROBLEM_LINKED` 时间线。

测试类固定先运行既有 MySQL 全链路，再运行并发用例，避免共享容器中的全局空表断言受反射顺序影响。该顺序本身不是并发正确性的来源；并发用例仍使用两个独立事务和线程。

## 4. 本地证据

- `mvnw -B package` 成功；共发现 42 项测试，40 项通过，0 失败，0 错误，2 项 MySQL 条件测试因本机 Docker 未启用按设计跳过。
- 测试源码重新编译成功，可执行 JAR 重新打包成功。
- 本机 Docker daemon 不可用，因此本地没有把 H2 结果写成 MySQL 通过。第二版修复后的 MySQL 2/2、前端、H2/JAR 和容器启动结论等待独立 Linux runner。
- 本轮没有前端变更；checkpoint 03/04 的桌面和移动页面证据保持原范围。

## 5. 首轮远端失败证据

- 提交 `6955cf8` 的 [GitHub Actions 运行 34104785177](https://github.com/Trigger726/OnCall-Agent/actions/runs/34104785177) 中，前端和 H2/JAR 通过，MySQL 门禁失败，容器门禁按依赖关系跳过。
- 并发用例在赢家主键已经被当前读回收后，于 `ProblemService.linkMatchingIncidents` 的普通查询抛出 `EmptyResultDataAccessException: expected 1, actual 0`。
- 这条失败直接证明“把一个查询换成当前读”不是完整修复，也验证了新增闸门确实覆盖 MySQL REPEATABLE READ 的旧快照行为，而非只在 H2 上制造表面绿色。

## 6. Demo 对比

历史 Demo 全部保留。checkpoint 03 证明产品层“一创一复用”，checkpoint 05 进一步展示数据库层的三段演进：旧回收路径依赖普通快照；第一版只把主键查询改成当前读但被真实 MySQL 击穿；最终版在新事务中完成完整恢复。面试时可用它解释为什么 H2 并发绿色不能自动推出 MySQL 隔离语义正确，也能展示如何保留失败证据并收紧修复边界。

## 7. 待完成

推送第二版后记录最终提交、MySQL 2/2 和四段式 CI 链接。若测试继续暴露死锁、超时或事务回滚，再按实际错误修复，不放宽屏障或删除并发断言换取绿色。
