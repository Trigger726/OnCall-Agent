# V1.7 checkpoint-15：真实双 JVM Agent 事件链路

日期：2026-09-12。状态：真实双 JVM 检查点通过，完整故障矩阵仍待完成。

## 目标与实现

新增独立条件测试 `DistributedAgentEventsIntegrationTest`：Testcontainers 启动 MySQL 8.4 与 Redis 7.4；两个独立 Java 子进程启动 OpsPilot Web 服务，使用不同 PID、端口、Spring 上下文、数据库连接池和 Redis 接收游标，共享同一数据库与 Stream。不是同一 JVM 内两个对象的替代测试。

A 通过真实登录/POST 接口发起调查，关闭发起 POST 的连接；A 的测试专用只读工具在文件闸门等待。A、B 分别通过带认证的 GET SSE 订阅，确认两端收到闸门步骤开始后才放行调查。只有测试工具的同步使用本地文件，事件传输必须经过生产 outbox/Redis/数据库/SSE 路径。

两个实例都将定期 catch-up 间隔与首次延迟设为一小时，真实用例在该时间窗内结束。关闭该兜底干扰后，后续终态必须由自动 relay/receiver 唤醒订阅；测试不手动调用 poll、relay 或 catchUp。生产默认首次延迟仍为 0，默认补读间隔仍为 5 秒。

断言两实例 PID 不同，A、B 从 0 读取的事件 ID 列表都与 B 的数据库回放接口完全一致、无重复且以 RUN_COMPLETED 结束。随后在 B 使用中间游标重连，验证只返回余下事件直到终态。测试工具仅在 src/test 中，不打入生产 JAR。

## 门禁与证据

运行：`./mvnw -B -Dopspilot.distributed.it.enabled=true -Dtest=DistributedAgentEventsIntegrationTest test`。

GitHub Actions 新增 Two JVM Agent SSE integration 独立门禁，容器构建依赖它通过。always 上传 `distributed-agent-evidence`：包含两个进程日志、result.json 的 PID/端口/runId/三份事件列表/恢复游标，以及 Surefire 报告。失败时日志尾部也进入测试输出。测试结束只终止本测试创建的子进程。

本地编译通过，但 Docker 引擎管道不可用；默认条件跳过 1 项不能计为真实双 JVM 验证成功。首轮 `658144d` 的双 JVM 门禁通过，A PID=3136/端口46689、B PID=3359/端口46305，三份事件列表均为 1–20，从游标 4 续订通过。[首轮原始结果](assets/V1.7-checkpoint-15/ci-34625652918-result.json) 保留。

复核发现长补读间隔仍允许首次立即调度；为严格排除其影响，增加首次延迟配置并在本用例设为一小时。同时关闭子进程 DevTools restart。首轮证据不能替代收紧后复验。子进程使用测试 classpath，不是两个发行版容器。

收紧后的代码提交 `20b1227` 在 [CI 34625976339](https://github.com/Trigger726/OnCall-Agent/actions/runs/34625976339) 六项门禁全部通过：双 JVM 1 分 13 秒、MySQL 1 分 4 秒、H2/JAR 1 分 2 秒、Redis 1 分 29 秒、前端 19 秒、容器 1 分 15 秒。双 JVM 条件用例 1 项执行通过、未跳过；默认后端 64 项，58 项通过、6 项条件跳过。

[收紧后原始结果](assets/V1.7-checkpoint-15/ci-34625976339-result.json)：A PID=2999/端口44759、B PID=3218/端口46521；run #1 的数据库/A/B 事件 ID 均为 1–20，无缺失或重复；B 从游标 4 恢复了 5–20。两个日志均显示 main 线程启动，无 ERROR。历史/最终 result.json 均已入库，其余完整日志可在对应 CI 的 distributed-agent-evidence 下载。

CI 有非阻断警告：upload-artifact@v4 的 Node 20 声明被 runner 强制切换至 Node 24，上传本轮成功；后续可独立升级并复验工作流依赖。

## Demo 对照与范围

保留原版源码备份及 checkpoint-14 的桌面/移动端新旧截图。checkpoint-14 证明单 Web 实例上的浏览器早 EOF 恢复；本检查点新增两个独立 Web 进程、真实 MySQL/Redis 和真实 HTTP SSE 的可重复自动化对照，不替代浏览器演示。

尚未覆盖运行实例崩溃后的任务接管、Redis 断开/重启/裁剪故障注入、真实慢客户端资源释放、跨实例浏览器自动重连。没有证明全部分布式验收矩阵，更不能声称任务会自动在 B 继续执行。
